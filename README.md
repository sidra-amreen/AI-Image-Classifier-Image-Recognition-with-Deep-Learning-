pip install streamlit opencv-python numpy tensorflow pillow

Package breakdown:

streamlit → Creates the web interface (import streamlit as st)
opencv-python → Image processing and resizing (import cv2)
numpy → Array operations (import numpy as np)
tensorflow → Deep learning framework + MobileNetV2 model
pillow → Opens and handles image files (from PIL import Image)
